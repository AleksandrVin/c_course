# This is language specs

## example

```c

func main() void{
    r = 10;
    b = 49;
    a = b + r;
    PrintNum(a);
    PrintNewLine();
    text string = "this is message";
    PrintText(string);
    while ( r < b )
    {
        r = r + 10;
    }
    if ( 10 > 5 )
    {
        PrintText("hello");
    }
}

```
